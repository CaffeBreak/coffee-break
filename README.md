# Caffe Break


## 必須要件
- Docker

## 環境構築
以下のコマンドを実行します
```sh
git clone https://github.com/CaffeBreak/coffee-break.git
cd coffee-break
docker compose up -d
```

## 使い方
フロントエンドサーバの起動
```sh
pnpm i
pnpm dev
```

## その他
開発は `develop` ブランチで行われています
