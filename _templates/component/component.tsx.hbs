import { NextPage } from "next";
{{{ propsType }}}

const {{ pascalCase name }}: {{{ typeAnnotation }}} = {{ props }} => {
  return <>{{ pascalCase name }}</>;
};

export default {{ pascalCase name }};
